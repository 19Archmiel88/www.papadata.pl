diff --git a/apps/api/src/modules/ai/ai.controller.ts b/apps/api/src/modules/ai/ai.controller.ts
index 107fb79..c43927d 100644
--- a/apps/api/src/modules/ai/ai.controller.ts
+++ b/apps/api/src/modules/ai/ai.controller.ts
@@ -11,7 +11,7 @@ import {
   UseGuards,
 } from '@nestjs/common';
 import type { FastifyReply, FastifyRequest } from 'fastify';
-import { SkipThrottle, Throttle } from '@nestjs/throttler';
+import { Throttle } from '@nestjs/throttler';
 import type { AIChatRequest, AIChatResponse } from '@papadata/shared';
 import { AiService } from './ai.service';
 import { getAppMode } from '../../common/app-mode';
@@ -31,7 +31,6 @@ const getRateLimitConfig = () => {
 };
 
 @Controller('ai')
-@SkipThrottle()
 export class AiController {
   private readonly logger = getLogger(AiController.name);
 
diff --git a/apps/web/components/LandingChatWidget.tsx b/apps/web/components/LandingChatWidget.tsx
index 17d38f2..9b94f50 100644
--- a/apps/web/components/LandingChatWidget.tsx
+++ b/apps/web/components/LandingChatWidget.tsx
@@ -192,6 +192,7 @@ export const LandingChatWidget: React.FC<{
                   value={input}
                   onChange={(e) => setInput(e.target.value)}
                   placeholder={copy.placeholder}
+                  aria-label={copy.placeholder}
                   className="flex-1 bg-black/5 dark:bg-white/5 border border-black/10 dark:border-white/10 rounded-2xl px-4 py-3 text-sm font-medium outline-none focus:border-brand-start/50 transition-all text-gray-900 dark:text-white"
                 />
                 <button
